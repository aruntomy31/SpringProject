plugins{
    id "base"
}

description = "A Sample SpringBoot Application"
version = "0.1-alpha"

task copy(type:Copy, group: "Custom", description: "Testing copy functionality" ){
    from "src/main/java"
    into "dest"
}

task zip(type: Zip, group: "Zip", description: "Testing zip functionality"){
    from "src"
    setArchiveName "test.zip"
}